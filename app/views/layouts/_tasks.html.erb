<% if tasks then %>
<% tasks = tasks.reject{|t| t.name==nil} %>

<% tasks.each do |task| %>

<% @task_status = "" %>
<% if task.status? 
  @task_status = "checked" 
end 
%>

<%= render 'layouts/task', :task=>task %>

<% end %>
<% end %> 
